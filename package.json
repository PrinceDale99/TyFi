{
  "name": "typhoon-resilience-vault",
  "version": "1.0.0",
  "description": "Root workspace control for TyFi",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:functions\" \"npm run dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:functions": "npm --prefix functions run dev",
    "postinstall": "npm install --prefix frontend && npm install --prefix functions",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "listener": "npm --prefix functions run listener",
    "install:all": "npm install --prefix frontend && npm install --prefix functions"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "xlsx": "^0.18.5"
  }
}
